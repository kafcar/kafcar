management:
  health:
    defaults:
      enabled: false
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health, shutdown, env
      base-path: /mgmt
